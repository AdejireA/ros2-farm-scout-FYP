# PROJECT STATUS — read this first, every time

**This file is the single source of truth for "where are we."** Not the chat history, not memory — this file. Update it every time you stop work, even mid-phase. If a fresh Claude Code session or a fresh version of me needs to pick this up, this file is all that should be needed — no replaying history.

---

## CURRENT STATE
*(the only section that needs updating constantly — everything below it is stable reference)*

**Active phase:** Phase 0 — Sanity check — COMPLETE, all 6 steps pass. Ready to start Phase 1.
**Doing right now:** Nothing — Phase 0 fully closed out.
**Last completed:** All of Phase 0:
  1. Build — PASS, 6/6 packages, zero errors/warnings.
  2. Sim launch — PASS. Was failing (Gazebo world load errored on `model://maize_01`/`maize_02`, server exited) because `~/ros2_ws/src/virtual_maize_field` was missing entirely — it's `.gitignore`d, never pushed to GitHub, and got wiped when the workspace was cleared. Fixed by re-cloning `https://github.com/FieldRobotEvent/virtual_maize_field.git` into `~/ros2_ws/src/`. User visually confirmed the world (rows/plants/obstacles/walls) and robot spawn look correct.
  3. Sensor topics — PASS. `/scan` ~2.5-2.9Hz (lower than TB3's typical ~5Hz, likely CPU contention from Gazebo GUI, not a functional issue), `/odom` ~17-18Hz, `/tf` publishing real transform data.
  4. Teleop — PASS. User confirmed WASD/arrows move the robot. Note: `keyboard_teleop`'s 0.4s dead-man's-switch (stops if no key pressed within 0.4s, so movement requires holding/repeating keys) is confirmed intentional design, not a bug — user explicitly chose to keep it as-is over switching to persist-until-next-key behavior.
  5. Mode arbitration — PASS. Defaults to `manual`, switches to `auto` and back to `manual` correctly via `/mode` topic.
  6. `scout_mission` binary — PASS. Runs and blocks waiting on `amcl/get_state` (expected — no Nav2/map yet), not a "package not found" or import error.
**Blocked on:** Nothing.
**Important operational notes carried forward:**
  - `simulation.launch.py` does NOT start `mode_arbitration`'s `arbitration_node` — without it running, nothing relays `/cmd_vel_teleop` → `/cmd_vel`, so teleop/Nav2 commands never reach Gazebo. Must be launched separately (`ros2 run mode_arbitration arbitration_node`) or added to a combined launch file until Phase 5's full_system.launch.py work happens.
  - `~/.bashrc` now auto-sources both `/opt/ros/jazzy/setup.bash` and `~/ros2_ws/install/setup.bash` (added this session) — new terminals just work, no manual sourcing needed. Existing open terminals need `source ~/.bashrc` once to pick it up.
  - `virtual_maize_field` stays untracked/gitignored by design — if the workspace ever gets cleared again, re-clone it from `https://github.com/FieldRobotEvent/virtual_maize_field.git` into `~/ros2_ws/src/`.
  - `MAX_LIN` in `keyboard_teleop.py` bumped from 0.30 to 0.50 m/s on 2026-08-14 to speed up manual SLAM-coverage driving. `MAX_ANG` left untouched at 0.80 rad/s. This is a teleop-only constant, not tied to any report figure or trial metric.
  - **Robot spawn point permanently changed from (0,−11) to (8,−10) on 2026-08-15** (center-south → south-east corner, near the last crop row / x=8). Files touched: `farm_world/worlds/farm.sdf` (robot `<pose>`, x/y only — z=0.2 and orientation untouched), `robot_bringup/scripts/scout_mission.py` (`SPAWN` constant). Verified via Gazebo's own `/world/farm_world/pose/info`: spawns at (8.00, −10.00), yaw≈0 (facing east, unchanged), clear of the south wall (~2m), the x=8 row's nearest plant (~1m), and the post obstacle at (0,−6) (~8.9m away).
  - `scout_mission.py`'s aisle sequence still always visits x=−8 first — with the new corner spawn, the mission's opening leg is now a full 16m west-bound traverse across the field (still through clear open space below all rows, not an error, just longer than before). Also, the new spawn point (8,−10) coincides exactly with aisle x=8's south waypoint, so the mission's final "return to spawn" leg is a straight backtrack down the same aisle it just came up — a harmless quirk of the corner spawn, not a bug.
  - `slam_coverage_drive.py` referenced in a task instruction does **not exist** in this repo (checked full workspace) — nothing to update there. If it gets added later, its `run_perimeter()` (or equivalent) will need to be written/re-derived for this corner start position and ~0° heading, not the old center-south assumption.
  - **README.md was NOT updated** — it still documents the old spawn point ((0,−11) in the "Field environment" section and the mission-profile diagram). This is now stale and should be fixed before Phase 5/11 report work references it.
**Next action:** Re-start Phase 1 (SLAM remap) from scratch at the current spawn point (−9.5,−10, facing north) — the earlier in-progress SLAM session was lost (background launch was orphaned/stopped, map never saved). Drive full coverage (perimeter first, then the 5 row aisles), save map, verify coverage numerically. Use `slam_coverage_drive` v3 (pose-checked, rewritten 2026-08-19, `LEG_TIMEOUT` already raised to 75s) — it reads the real starting pose via TF rather than assuming one, so it does not need updating for the spawn change itself.
- **Fixed default Gazebo camera added to `farm_world/worlds/farm.sdf` on 2026-08-18** — a `<gui>` block (Gazebo Harmonic's real `MinimalScene` plugin structure, verified against the gz-sim8 worlds actually installed under `/opt/ros/jazzy/opt/gz_sim_vendor/share/gz/gz-sim8/worlds/`, not guessed) with `<camera_pose>0 0 32 0 1.570796 1.570796</camera_pose>` — top-down, north-up: camera sits at field-centre (0,0), 32 m up, pitched straight down and yawed so **+y (north) = up on screen, +x (east) = right**. Verified by launching `simulation.launch.py` and screenshotting the window: field renders as a true undistorted rectangle (no more oblique default angle) and the robot, spawned at (8,−10), appears exactly in the **bottom-right** of the view as expected. "Top of screen = north" is now reliable **for a launch where the fix is actually in effect** — see the caveat immediately below, it is not unconditionally reliable yet.
  - **Important caveat — read before assuming the camera fix "just works":** Gazebo Sim caches the GUI's camera pose at `~/.gz/sim/8/gui.config` (a per-machine file, not in this repo) and **that cached file silently overrides `farm.sdf`'s `<gui>` block whenever it exists.** Discovered because the first post-fix test still showed the old oblique angle until this cache was deleted. Worse: **gz-sim rewrites this same file with the old default oblique pose (`-6 0 6 0 0.5 0`) every time the Gazebo window closes normally** — this reproduced even on a launch where the camera was never touched, so it isn't just "if someone orbits with the mouse," it's every ordinary close. Net effect: the farm.sdf fix reliably produces the top-down view on the *next* launch only if `~/.gz/sim/8/gui.config` doesn't exist at that moment.
    - **If the camera reverts to an arbitrary angle in a future session:** run `rm -f ~/.gz/sim/8/gui.config` before `ros2 launch robot_bringup simulation.launch.py`. Confirmed safe/effective — this is exactly what fixed it this session.
    - **Root cause of the original "which way is north" confusion:** that cache file's timestamp was 2026-08-14 09:20 — the very first Gazebo run of this project. Someone (or the plugin itself) orbited/closed the window that day and it's been silently overriding every world's camera ever since, project-wide, not specific to farm.sdf.
    - **Not fixed here (out of scope for this task):** a permanent fix would mean patching `simulation.launch.py` to pass `--gui-config` or delete the cache automatically before each launch — deliberately not done, since the task scope was "the `<gui>` camera addition" only, nothing else in the launch chain.
- **SLAM failure mode #2 — large map→odom jump, diagnosed 2026-08-19, NOT FIXED, session abandoned per user instruction (report only, no correction attempted).** This is separate from failure mode #1 (scan-match drift / doubled box, from an earlier Phase 1 attempt). Session: `ros2 launch robot_bringup full_system.launch.py use_slam:=true`, started 02:06:38, `slam_toolbox` PID 65029 activated 02:06:48.
  - **Symptom:** mid-drive, `tf2_echo map base_link` reported the robot at **x=−14.985, y=22.520** — ~4 m past the west wall (x=−11) and ~10 m past the north wall (y=12), physically impossible in the fully-enclosed field.
  - **Root cause isolated to `map`→`odom`, not `odom`→`base_link`.** Checked both legs separately at the same moment: `odom`→`base_link` = (−6.723, 1.419), yaw −68.3° — a locally sane, continuous number, consistent with honest wheel/IMU odometry. `map`→`odom` = (**−18.174, 16.434**), yaw **−105.7°** — grossly non-identity, and this is what corrupts everything downstream. Composing the two reproduces the reported bad position. **This means slam_toolbox's global localization estimate (where it thinks the odom frame sits in the map) jumped, not the short-term odometry tracking.**
  - **The bad `map`→`odom` value is frozen, not actively drifting:** polled 5× over ~0.9 s, identical every time. The corruption already happened and stuck — it is a discrete state, not ongoing drift at the moment it was caught.
  - **Onset pinpointed via `global_costmap` (nav2) logs, since `slam_toolbox`'s own log file contains only 6 lines for the entire 53-minute session** (stack-size/config/activation messages + one unrelated WARN — see below) **and never logs loop-closure or scan-match events at INFO level, so there is no direct internal record of what triggered this.** Circumstantial evidence from `~/.ros/log/latest/../planner_server_65024_*.log`:
    - Steady, incremental `StaticLayer: Resizing costmap` growth from launch until **02:42:13** (442×646, stable — width hadn't moved since ~02:12).
    - **02:42:48 (35m59s after slam_toolbox activation): `[WARN] [nav2_costmap_2d]: Robot is out of bounds of the costmap`** — this is the onset timestamp.
    - 02:42:50–52: sensor origin reported at (4.26, 20.36)→(4.45, 20.42) — already past the north wall.
    - 02:42:53 & 02:42:58: costmap width jumps 462→522→523 (+3 m), consistent with the map suddenly needing to cover a far-flung new position — matches the west/north direction of the bad `map`→`odom` translation.
  - **The "Invalid frame ID 'map' — frame does not exist" warning is a red herring, not evidence of a restart.** It's `tf2_echo`'s own standard ~1-cycle startup message (prints every time the tool starts, before its TF buffer fills, on every invocation — reproduced on every `tf2_echo` call run during this diagnosis, including totally healthy ones). **Confirmed no slam_toolbox restart occurred:** same PID (65029) continuously since 02:06:48 (`ps` uptime matches launch time exactly), and `launch.log` has exactly one `process started` line for `async_slam_toolbox_node` — no crash/respawn.
  - **Unrelated but worth fixing separately — FIXED 2026-08-19:** `slam_toolbox` logged one config WARN at activation: `maximum laser range setting (12.0 m) exceeds the capabilities of the used Lidar (3.5 m)`. Not confirmed as the cause of this jump (no log evidence ties them together), but a real misconfiguration — over-claiming free space beyond the sensor's real range is a plausible contributor to bad scan matches. Corrected in `nav2_config/config/nav2_params.yaml`: `max_laser_range: 12.0` → `3.5` (line 359, slam_toolbox's own config — the one actually producing the WARN). Also corrected the matching mismatch in AMCL's config in the same file, `laser_max_range: 12.0` → `3.5` (line 30) — same TB3 Waffle LiDAR (3.5 m real range), same bug, just not yet triggered since this session used `use_slam:=true` (AMCL isn't active in SLAM mode); would have hit the identical warning in Phase 2 (AMCL test) otherwise. Rebuilt (`colcon build --packages-select nav2_config`) and verified the installed copy under `~/ros2_ws/install/nav2_config/...` reflects `3.5` at both lines.
  - **Real-time tripwire for next drive:** watch `planner_server`'s log (or its terminal output) for `[WARN] [nav2_costmap_2d]: Robot is out of bounds of the costmap` — that's the exact message that flagged this failure, and it fired the same second the jump happened (02:42:48), not after the fact. If it appears again, stop and check `tf2_echo map odom` immediately rather than continuing to drive on a corrupted map frame.
  - **What was NOT attempted this round (session diagnosed 2026-08-19):** no correction, no restart, no config change to the SLAM session itself — per explicit instruction, that session was left as-is for inspection only, and has since been torn down (`pkill -9` on gazebo/gz/rviz2/ros2, confirmed clean via `ps aux`). The `~/.gz/sim/8/gui.config` camera cache was deleted again ahead of the next launch (see the caveat above — it doesn't stay fixed on its own). Next launch/drive is the user's, in shorter checkpoints this time rather than one long unattended session.
- **Manual SLAM-coverage driving set aside 2026-08-19 in favor of a scripted approach**, after 3 manual attempts hit 3 different failure modes: (1) scan-match drift from fast turns, doubled/duplicated box in the map; (2) the discrete map→odom pose-graph jump documented above; (3) incomplete coverage (south/east edges came up short) despite a drive that looked clean and closed visually. Manual driving isn't ruled out permanently, but the next attempt is scripted first.
  - **`robot_bringup/scripts/slam_coverage_drive.py` created and installed 2026-08-19.** Installed via `CMakeLists.txt` (`install(PROGRAMS scripts/slam_coverage_drive.py RENAME slam_coverage_drive DESTINATION lib/${PROJECT_NAME})`, same pattern as `scout_mission.py`), rebuilt clean (`colcon build --packages-select robot_bringup`), registration confirmed (`ros2 pkg executables robot_bringup` lists it; a 4s hard-timeout run showed no import/package-not-found error and left no stray process). Run: `ros2 run robot_bringup slam_coverage_drive` (pauses between the perimeter and aisle stages) or `--auto` (runs straight through). Requires SLAM already up (`full_system.launch.py use_slam:=true`) and mode set to `manual`.
  - **NOT YET RUN LIVE — reviewed only.** Two real logic problems were found reading the installed copy, neither fixed (out of scope for this task — file content was to be installed exactly as given):
    1. **The TF buffer goes stale during every motion segment.** `TransformListener` is constructed with the default `spin_thread=False`, so it depends on this node's own executor spinning to receive `/tf`. But the motion loops (`_publish_for`/`_stop`) only call `time.sleep()`, never `rclpy.spin_once()`, after the one-time 20-iteration warm-up in `__init__`. Practical effect: `sanity_check_pose()` — the mechanism specifically built to catch the map→odom-jump failure mode — reads a buffer that stopped updating right after construction, and its own `None`-pose branch treats "can't check" as "don't block." **This likely means the safety check the whole rewrite is built around would silently pass even during a real jump.** Needs fixing (e.g. `spin_thread=True` on the `TransformListener`, or a `spin_once` call inside the motion loops) before this is trustworthy.
    2. **`stage_aisles()` has no transition leg from where `stage_perimeter()` ends** (~(8,−10) facing east) **to where the aisle loop assumes it starts** (x=−8, facing north/south) — it just starts driving forward from whatever heading is active. Traced through: this would drive the robot east toward the already-close east wall on the very first move, likely triggering `sanity_check_pose`'s own abort rather than covering any aisle. Separately, even with that fixed, the inter-aisle jog (`turn(90 if i%2==0 else -90)`, called twice per transition) doesn't net an eastward step through `AISLES = [-8,-4,0,4,8]` given the actual alternating north/south heading — it nets westward instead.
  - The perimeter-stage math itself (headings, inset distances, loop closure) was traced by hand and is internally consistent — the problems found are confined to the TF-spin gap and the aisle-stage transition/turn-direction logic.
  - **Second review, 2026-08-19 — two targeted fixes applied to the existing file (edited in place, not recreated), rebuilt, re-reviewed. STILL NOT RUN LIVE.**
    - **Fix 1 (TF staleness) — CONFIRMED GENUINELY FIXED, not just "less likely."** Every `time.sleep(self._period)` in `_publish_for()`/`_stop()` replaced with `rclpy.spin_once(self, timeout_sec=self._period)`, so the executor spins continuously through every motion segment instead of only during `__init__`'s one-time warm-up. Checked for a threading hazard specifically (`TransformListener` still has `spin_thread=False`, no second spinner exists) — none. One real nuance, not a flaw: `spin_once()` dispatches at most one callback per call, and `/tf` publishes faster than 10 Hz (slam_toolbox's `transform_publish_period: 0.02` = 50 Hz), so the buffer can lag mid-leg during a long straight — but `sanity_check_pose()` only fires at leg/stage boundaries, by which point continuous draining has caught it up. Fixes the check for how the script actually uses it.
    - **Fix 2 (aisle transition/turn-direction) — PARTIALLY FIXED, one bug remains, confirmed by a full step-by-step angle trace (not just inspection).** Traced from `stage_perimeter()`'s real ending state — (8,−10), heading east (0°), confirmed by retracing all 4 perimeter legs — through the new transition block: `turn(90)→90°(N)`, `turn(90)→180°(W)`, `forward(16)→x:8→-8`, `turn(90)→270°(S)`. **Lands on the correct position (x=−8) but the wrong heading (south, not north)** — three left turns net +270°, where two lefts + one right (net +90°=north) was needed. `heading_is_north=True` is then asserted immediately after but is factually wrong. Consequence: the first `forward(19)` in the aisle loop would drive south from y=−10 — 2m from the south wall — instead of north up the row, almost certainly re-triggering `sanity_check_pose()`'s abort on the very first aisle, just relocated from the old bug's failure point. **The boustrophedon turn-direction logic itself (the `heading_is_north` state machine driving the 5-aisle sweep) was separately traced assuming a correct starting heading and is internally consistent** — alternates north/south correctly and steps +4m east through all 5 aisles in order. So this fix did correct the original wrong-turn-direction defect; it introduced one new, narrower bug confined to the transition block (last `turn(90)` should be `turn(-90)`). Not applied — report only, per instruction.
    - Rebuilt clean (`colcon build --packages-select robot_bringup`), syntax-checked (`py_compile`), confirmed installed copy matches source exactly (`diff` clean).
  - **Third review, 2026-08-19 — one-line correction applied, re-traced, and given a full final read-through. READY TO RUN LIVE. Still not actually run.** `stage_aisles()`'s transition block: third turn changed from `self.turn(90)` to `self.turn(-90)` (two lefts to reach west from the perimeter's east-facing end, one right to reach north — not a third left). Rebuilt, re-traced angle-by-angle same method as the last two reviews: from confirmed perimeter end-state (8,−10)/east(0°) → `turn(90)→90°(N)` → `turn(90)→180°(W)` → `forward(16)→x:8→-8` → `turn(-90)→90°(N)`. **Confirmed: now correctly lands at (-8,-10) facing north**, matching `heading_is_north=True`. Re-traced the full 5-aisle boustrophedon under this now-correct start too — steps cleanly through x=-8,-4,0,4,8 in order, full north/south sweep each time, no more drift. Then did a complete end-to-end read of the whole file (not just the diff) — perimeter math, both TF-liveness fixes, `check_full_coverage()`, exception handling, `main()` sequencing all re-checked clean; confirmed old buggy `90 if i % 2` pattern fully gone via grep; syntax-checked and source/installed-copy parity re-confirmed. Two cosmetic-only findings, not blockers: `inset_south` (stage_aisles) and module-level `START_HEADING_DEG`/`LIDAR_RANGE` are computed/defined but never used anywhere — dead code, no functional effect. **No remaining logic problems found — this is the first review of this script to end with a clean yes.**
  - **v2 actually run live 2026-08-19 (implied by this rewrite) — produced a diagonal-sliver map, not a clean field outline.** Root cause: v2's `forward()`/`turn()` never checked real pose mid-leg, only fixed `distance/speed` durations computed up front — any drift (scan-match, timing, wheel slip) compounded silently across all 8 perimeter legs plus every aisle leg, with no correction. The pose checks (`sanity_check_pose`) only ran *between* legs, so a leg that drifted but still landed inside the generous `+-3m` sanity margin passed the check while already off-course, and the next leg's fixed duration carried that error forward.
  - **Rewritten v2 → v3 2026-08-19 — file replaced entirely (not edited in place), per exact content supplied.** v3 changes the fundamental drive strategy: no leg ever assumes a distance traveled. `drive_until(axis, target)` polls real `map->base_link` pose every publish cycle and stops when the axis value reaches target within `POSITION_TOLERANCE` (0.3 m) — self-correcting against drift instead of compounding it. `turn_to(target_deg)` commands an **absolute compass heading** (0=E, 90=N, ±180=W, −90=S) read from live yaw via quaternion `atan2`, replacing v2's relative `turn(±90)` calls — removes the entire class of turn-direction bugs the last two reviews spent effort chasing (v2's transition-block turn-direction defect literally cannot recur in v3, since there's no accumulated relative-turn state to get wrong). Also drops the hardcoded `SPAWN`/`START_HEADING_DEG` constants entirely — `stage_perimeter()` reads whatever the actual starting pose is via TF and logs it, and `stage_aisles()` picks its starting aisle by proximity to current x rather than assuming x=−8. Rebuilt clean (`colcon build --packages-select robot_bringup`), registration confirmed (`ros2 pkg executables robot_bringup` lists it; a 4s hard-timeout run produced no import/registration error and left no stray process), installed copy confirmed identical to source (`diff` clean).
  - **Review of the installed v3 copy, 2026-08-19 — NOT run live. One real, confirmed logic problem found; two items checked clean:**
    1. **`GetMap`/`map_server/map` service usage — confirmed correct for this ROS2 Jazzy / Nav2 install.** Checked `/opt/ros/jazzy/include/nav2_map_server/map_server.hpp` directly: the map_server node advertises a *relative* service named `"map"` of type `nav_msgs::srv::GetMap`; `navigation.launch.py` instantiates that node with `name='map_server'` (confirmed by grep), so the fully-qualified service is `/map_server/map` — matches the script's `create_client(GetMap, 'map_server/map')` exactly. No fix needed.
    2. **`turn_to()`'s yaw math — confirmed correct.** `atan2(2*(w*z+x*y), 1-2*(y*y+z*z))` is the standard planar (Z-axis-only) quaternion→yaw formula; correct for a differential-drive robot on flat ground.
    3. **`LEG_TIMEOUT = 45.0` is too tight — CONFIRMED, not just plausible.** At `LIN_SPEED = 0.35 m/s`, the two long perimeter legs (spawn y → `north_target=+10`, then `+10` → `south_target=-10`) are 20 m each → **57.1 s** of actual driving needed; the aisle sweep legs (`Y_SOUTH=-9.5` → `Y_NORTH=9.5`, the very traversal the docstring calls "~19m across the field") are 19 m → **54.3 s**. Both exceed the 45 s cap. Since `drive_until()` returning `reached=False` on timeout causes `stage_perimeter()`/`stage_aisles()` to abort (`if not self.drive_until(...): return False`), **as configured, every real run will very likely abort partway through leg 1 of the perimeter stage** (or leg 3, if leg 1 happens to run a bit fast) before ever reaching the aisles — this is not an edge case, it's the expected outcome of the two longest, most important legs in the whole script. **Not fixed — flagged only, per instruction not to run or further modify the file.** If this bites on the live run, the fix is either raising `LEG_TIMEOUT` (60s+ covers both cases with margin) or raising `LIN_SPEED`, not a logic rewrite.
  - **`LEG_TIMEOUT` raised 45.0 → 75.0, 2026-08-19** — the fix for the finding directly above, applied on request (single-constant change only). Now clears both the ~57.1s perimeter legs and the ~54.3s aisle-sweep legs with real margin. Rebuilt (`colcon build --packages-select robot_bringup`), confirmed present in both source and installed copy (`grep` on both, line 63 and its one usage at line 177). Nothing else in the file touched.
  - **Robot spawn permanently changed again, 2026-08-19: (8,−10) facing east → (−8,−10) facing north (yaw=π/2), to simplify the SLAM perimeter loop to all-right-turns and start the aisle sweep already at the first crop row.** This supersedes the 2026-08-15 spawn-change entry above — that entry's specifics (facing east, southeast corner) no longer describe the current spawn.
    - **Correction to the 2026-08-15 entry's file list, discovered while making this change:** the robot's spawn `<pose>` was never actually a standalone line in `farm_world/worlds/farm.sdf`'s early section — it's set on the `<include>...<uri>model://turtlebot3_waffle</uri>...<pose>` block near the end of the file (after all the maize `<include>` rows), around what is now line 348. The 2026-08-15 note describing this as "robot `<pose>`, x/y only" was directionally right but didn't record the exact block; noting it here since it took a targeted grep to relocate.
    - `farm_world/worlds/farm.sdf` line 348: `<pose>8 -10 0.2 0 0 0</pose>` → `<pose>-8 -10 0.2 0 0 1.5708</pose>` (x: 8→−8, y/z unchanged, yaw: 0→1.5708 rad = π/2 = facing +y/north).
    - `robot_bringup/scripts/scout_mission.py` line 26: `SPAWN = (8.0, -10.0)` → `SPAWN = (-8.0, -10.0)`. (Its "return to spawn" waypoint already used `math.pi / 2` for its heading — line 49 — so that line needed no change; it was presumably written ahead of this spawn correction.)
    - Full workspace rebuild (`colcon build`, 7/7 packages, no errors/warnings) — not just `robot_bringup`, since `farm_world` also changed.
    - **Verified live**, not just by inspection: deleted `~/.gz/sim/8/gui.config` (the camera-pose cache — see the 2026-08-18 caveat above), launched `simulation.launch.py`, and queried `gz topic -e -t /world/farm_world/pose/info -n 1`. Result for `turtlebot3_waffle`: position `(x=-8.00, y=-10.00, z=0.01)`, orientation quaternion `(x≈0.0020, y≈-0.0020, z≈0.70711, w≈0.70710)` — this composes to yaw ≈ 1.5708 rad (90.00°), i.e. facing true north (+y). The small x/y quaternion components and z=0.01 (vs. the spawned z=0.2) are ordinary settle-onto-ground physics noise from the drop, not a problem.
    - **Housekeeping found and cleared during verification, unrelated to this change:** an orphaned `gz sim server` process (PID 80248, started 04:40, no parent `ros2 launch` — leftover from an earlier session in the day) was still running and would have made the pose-topic query ambiguous (two servers publishing the same world's pose topic). Killed before verifying. The verification launch itself was torn down afterward (`kill -9` on its `gz sim`/`ros2 launch` PIDs) — confirmed clean via `ps aux`, nothing simulation-related left running.
    - **Not yet updated to match:** `README.md` still documents the pre-2026-08-15 spawn point (0,−11) — already flagged stale above, now additionally stale on spawn location/heading twice over. `scout_mission.py`'s aisle-visit order (`AISLES = [-8.0, -4.0, 0.0, 4.0, 8.0]`, starts at −8) now matches the new spawn almost exactly (spawn is at x=−8, so the mission's first waypoint is directly ahead rather than a 16m cross-field traverse like before) — this is a nice side-effect, not something that needed changing. `slam_coverage_drive.py` v3 needs no change for this — it reads real starting pose/heading via TF rather than assuming one, by design.
    - **DID NOT TOUCH:** SLAM map files (none exist currently — `nav2_config/maps/` is empty per the audit), `nav2_config/config/nav2_params.yaml`, `farm.sdf`'s layout (rows/walls/obstacles/camera `<gui>` block — only the robot's own `<pose>` line changed), or the report.
  - **Spawn x nudged again, 2026-08-19: (−8,−10) → (−9.5,−10), same heading (north, yaw=1.5708).** Purpose: −8 sat directly in line with the first crop row (x=−8); −9.5 centers the robot in the 3m gap between the west wall (x=−11) and that row instead. y, z, and yaw all unchanged from the immediately preceding entry.
    - `farm_world/worlds/farm.sdf` line 348: `<pose>-8 -10 0.2 0 0 1.5708</pose>` → `<pose>-9.5 -10 0.2 0 0 1.5708</pose>`.
    - `robot_bringup/scripts/scout_mission.py` line 26: `SPAWN = (-8.0, -10.0)` → `SPAWN = (-9.5, -10.0)`.
    - Rebuilt `farm_world` + `robot_bringup` only (not a full workspace build this time — nothing else changed).
    - **Verified live**, same method as the previous spawn change: cleared `~/.gz/sim/8/gui.config`, launched `simulation.launch.py`, queried `/world/farm_world/pose/info`. Result: `turtlebot3_waffle` at `(x=-9.50, y=-10.00, z=0.01)`, orientation quaternion unchanged from the prior verification (`z≈0.7071, w≈0.7071`, yaw≈90°/north) — confirms only x moved, heading is exactly as before. Verification launch torn down cleanly afterward (confirmed via `ps aux`).
    - No stale/orphaned Gazebo process this time (unlike the previous spawn-change verification) — none found before launching.
    - **Still not updated to match, now three spawn-location revisions behind:** `README.md`'s spawn point (still documents the original (0,−11)). `scout_mission.py`'s aisle order (`AISLES = [-8.0, -4.0, 0.0, 4.0, 8.0]`) still starts at x=−8 — spawn is now 1.5m further west than the first aisle rather than exactly on it, so the mission's opening leg is a short ~1.5m eastward step to reach x=−8 before turning to sweep it, not zero-distance like the previous spawn. Not a bug, just worth knowing before assuming the first leg is instantaneous. `slam_coverage_drive.py` v3 again needs no change — reads real pose via TF.
    - **DID NOT TOUCH anything else**, per instruction.
  - **`slam_coverage_drive.py`'s `AISLES` changed from row positions to gap centerlines, 2026-08-19.** `AISLES = [-8.0, -4.0, 0.0, 4.0, 8.0]` was identical to the crop-row x-positions in `farm.sdf` (`maize_r*` rows at x=-8,-4,0,4,8) — the aisle sweep was driving straight down the middle of each row, not scouting alongside it. Changed to `AISLES = [-6.0, -2.0, 2.0, 6.0]` — the 4 gap centerlines, each 2m from its two flanking rows, comfortably inside `LIDAR_RANGE=3.5m` so both flanking rows stay visible to the scan without the robot ever driving through one. One-line comment added directly above explaining this (gap centerlines, not row positions). Nothing else in the file touched — `Y_SOUTH`/`Y_NORTH` unchanged, `stage_aisles()`'s nearest-aisle-first selection logic needed no change (it already picks the starting lane dynamically off whatever `AISLES` contains). Rebuilt (`colcon build --packages-select robot_bringup`), confirmed in both source and installed copy.
    - **Known now-stale leftover, deliberately not touched:** the module docstring (line 23) still reads "AISLES — serpentine across the 5 aisle lines (x = -8,-4,0,4,8)" — describes the old row-aligned values, now wrong (4 lanes, not 5; different x's). Left as-is per explicit instruction to change only the `AISLES` line and its one comment, nothing else in the file.
  - **`scout_mission.py` inspected, NOT edited (per instruction) — confirmed it has its own, separate, still row-aligned aisle list.** It defines `AISLES = [-8.0, -4.0, 0.0, 4.0, 8.0]` at line 23 (own copy, not shared/imported from `slam_coverage_drive.py`) and builds explicit Nav2 `PoseStamped` goals from it in `build_waypoints()` — boustrophedon through each of those 5 x-values from `Y_SOUTH=-10.0` to `Y_NORTH=10.0` alternating direction, plus a final return-to-`SPAWN` waypoint. **This still matches the OLD row-aligned list exactly** — `scout_mission.py` was not part of this task's `AISLES` fix and still drives the robot straight down each crop row's centerline, same collision exposure as `slam_coverage_drive.py` had before today. Its own module docstring (line 9: `Aisle x positions: -8, -4, 0, 4, 8`) and header comment agree with its code — internally consistent, just not yet updated to the gap-centerline approach. Left entirely unmodified, decision on how to fix it deferred to the user.
  - **Maize plant collision geometry checked — driving row-aligned WAS a real physical collision risk, not just a mapping/LIDAR quality issue.** Located via `model://maize_01` / `model://maize_02` in `farm.sdf` → `~/ros2_ws/src/virtual_maize_field/models/maize_01/model.sdf` and `.../maize_02/model.sdf` (both used alternately for the 50 plants, 5 rows × 10). **Both define a real `<collision>` element**, not visual-only: a `0.02 × 0.02 × 0.4 m` box (thin stalk shape) at `<pose>0 0 0.2 0 -0 0</pose>`, with contact/friction surface parameters (`<mu>10</mu>`, `max_contacts=10`) — a normal physically-simulated Gazebo collision body, alongside a separate mesh-based `<visual>` element (the `.dae` model) that isn't what's used for physics. So the old row-aligned `AISLES` path in `slam_coverage_drive.py` was driving the robot's footprint directly through a line of real collision boxes, not just degrading scan quality — this was a genuine bump/knock-over risk each time it crossed a plant, not merely a LIDAR occlusion problem. This is the same physical fact for `scout_mission.py`'s still-unfixed row-aligned path above.
  - **`scout_mission.py`'s `AISLES` fixed to match, 2026-08-19 — this is the higher-stakes fix of the two, since this is the actual trial-running script.** Same bug, same collision exposure confirmed above: `AISLES = [-8.0, -4.0, 0.0, 4.0, 8.0]` (line 23, own separate copy — not shared with `slam_coverage_drive.py`) was identical to the crop-row x-positions, meaning **every evaluation trial run to date has driven the robot's footprint straight through a line of real `<collision>` boxes** (confirmed physical, not visual-only — see the finding directly above), not just a mapping artifact. Changed to `AISLES = [-6.0, -2.0, 2.0, 6.0]` — same gap-centerline values as `slam_coverage_drive.py`, same 2m-from-each-row / within-LIDAR-range reasoning, same one-line comment added above the constant. **Unlike `slam_coverage_drive.py` (where the stale docstring was deliberately left alone), this script's docstring/header WAS updated to stay accurate** — since it's the trial-running script: retitled "boustrophedon traversal of all 5 row aisles" to "...of the 4 inter-row aisles", replaced the old `Aisle x positions: -8, -4, 0, 4, 8` line with the new values plus the same gap-centerline explanation, kept the still-correct `Y travel range` line as-is.
    - **Verified `build_waypoints()` fully regenerates from the new `AISLES`, no leftover hardcoded row positions:** the function loops `for i, x in enumerate(AISLES)` with no literal x-values anywhere in its body — confirmed by reading the whole file, not just grepping. The only remaining `-8,-4,0,4,8` text in the file is in the docstring, and it correctly describes the crop rows for context, not used as a coordinate anywhere in code. Side effect worth knowing: the mission now generates **9 waypoints** (4 aisles x 2 ends + 1 return-to-spawn), not 11 as before (5 x 2 + 1) — any report figure or trial-metric script that assumes 11 waypoints needs updating to match, once one exists (Phase 7).
    - Rebuilt (`colcon build --packages-select robot_bringup`), confirmed source/installed copy identical (`diff` clean). **Not run** — per explicit instruction, since this script drives real evaluation trials; left for the user to review the diff and run themselves.
  - **`Y_SOUTH`/`Y_NORTH` mismatch between the two scripts checked, REPORT ONLY, neither file changed:** `scout_mission.py` uses +/-10.0, `slam_coverage_drive.py` uses +/-9.5. **Neither risks the north/south wall — this is an inconsequential mismatch relative to the wall, not a safety issue.** `slam_coverage_drive.py`'s own perimeter-stage math (`WALL_Y=12.0`, `PERIMETER_INSET=2.0`, giving `north_target/south_target = +/-10.0`) confirms the wall itself is at y=+/-12; even the larger of the two aisle values (+/-10.0) still stops 2.0m short of the wall, and +/-9.5 stops 2.5m short — both comfortably safe. The actual difference the 0.5m mismatch affects is plant clearance, not wall clearance: crop rows span y=+/-9 (per `farm.sdf`'s own comment, "y = -9 to 9"), so `slam_coverage_drive.py`'s +/-9.5 leaves only 0.5m past the outermost plant row while `scout_mission.py`'s +/-10.0 leaves 1.0m — a minor inconsistency in how tight the sweep hugs the last plant in each row, not a bug and not touched.
  - **De-duplicating `AISLES` (and `SPAWN`/`Y_SOUTH`/`Y_NORTH`) into one shared constants module between `slam_coverage_drive.py` and `scout_mission.py` is explicitly deferred — a Day 6 cleanup item, not done now.** Both scripts currently carry their own independent copies of these values by design (mirrored fix applied to each separately, not refactored) — a future editor of either file needs to remember to check the other one too until that cleanup happens.
- **CRITICAL FINDING, 2026-08-19: `slam_coverage_drive.py` v3 compared local SLAM map-frame pose to Gazebo world-frame constants — two different coordinate systems. Rewritten to v4 (relative pose tracking). Not yet run live.**
  - **Root cause, confirmed live by the user via `ros2 run tf2_ros tf2_echo map base_link`:** at startup this reports position `(0, 0, 0.12)`, yaw `0` — NOT the real spawn `(-9.5, -10.0)`, yaw 90°. This is correct `slam_toolbox` behavior (the `map` frame's origin is defined as wherever the robot is when SLAM activates, independent of Gazebo's world frame), but v3's every threshold (`WALL_Y=12.0`, `AISLES=[-6,-2,2,6]`, `PERIMETER_INSET`, etc.) was written using Gazebo WORLD-frame numbers from `farm.sdf`, while `get_pose()` reads the LOCAL map frame starting at `(0,0,0)`. As written, every `drive_until()` call would have stopped after a few metres of local-frame travel instead of the real ~18-20m needed to reach the actual field boundary — silently undercovering the field, no error raised. **Never run live in this state**, per the user's explicit instruction before making any change.
  - **Fix: rewrote `robot_bringup/scripts/slam_coverage_drive.py` v3 → v4, relative pose tracking instead of absolute-coordinate comparison.** Design kept identical (pose-checked legs, not fixed-duration; live TF polling every tick; self-correcting within each leg) — only what "target" means changed:
    - `turn_to(target_deg)` (absolute compass heading) → `turn_relative(delta_deg)`: captures live yaw at call time, computes `target = current_yaw + delta_deg`, then reuses the exact same polling/correction loop.
    - `drive_until(axis, target)` (absolute-axis comparison) → `drive_relative(distance_m)`: captures live `(x0,y0)` at call time, polls pose every tick, stops when Euclidean distance traveled from that snapshot ≥ `distance_m` (within `POSITION_TOLERANCE`).
    - Both still poll real pose every tick and self-correct for drift mid-leg — only the reference point moved from "world-frame constant" to "wherever this leg started."
  - **Leg distances/turns are computed at runtime from named constants (`WALL_X`, `WALL_Y`, `PERIMETER_INSET`, `AISLES`, `Y_SOUTH`, `Y_NORTH`) and new constants `SPAWN_X=-9.5`, `SPAWN_Y=-10.0`, `SPAWN_YAW_DEG=90.0`** (the real, current spawn — pulled live from `farm.sdf` during this task, not the `(-8,-10)` mentioned in the request, which is one spawn-revision stale). An internal "planned pose" trail (`self._plan_x/_plan_y/_plan_yaw`), seeded from these constants, is advanced by each commanded delta and used purely for the existing decision logic (turn-direction-by-comparison, nearest-aisle-first selection, N/S alternation) — kept 100% structurally unchanged, only the underlying motion primitives changed. Traced by hand: perimeter legs come out to 20.0m (N) / 18.5m (E) / 20.0m (S) / 18.5m (W), all turns after the first are −90° (right turns, first turn is a genuine no-op since spawn already faces north) — matches the perimeter's known "all right turns" design. Aisle stage: first lateral shift 3.5m, then three 4.0m shifts; sweeps 19.5m then three 19.0m sweeps (the 0.5m difference on the very first sweep is the already-documented `Y_SOUTH`/`Y_NORTH` vs. perimeter-end-y mismatch, correctly carried through, not new).
  - **One deliberate deviation from the request's literal wording, flagged explicitly:** the request said to call `turn_relative(90, ...)` for the north leg. Traced through: since the robot is already facing north at spawn (yaw=90° confirmed live), a +90° relative turn on the very first command would swing it to face **west** instead, immediately wrong. Implemented `turn_relative(Δ=0)` for that first turn instead (computed as `target_heading(90) - planned_yaw(90) = 0`) — a genuine no-op, consistent with the perimeter loop's known "starts already aligned, all-right-turns" design. Flagging this rather than silently deviating.
  - **`sanity_check_pose()` reframed**, per instruction, since raw `(x,y)` vs. world-frame wall bounds is meaningless in local frame: now tracks CUMULATIVE distance traveled (odometer-style, summed tick-by-tick) since the first pose logged at startup, flags if it exceeds a ceiling. **Ceiling set to 260m, not the request's "~90m" example** — the real planned-path total, computed from the same known geometry, is ~169m (perimeter 77m + aisles 92m); 90m is already less than that and would have false-triggered on a clean run. 260m gives ~50% margin above the honest total. Also added (beyond the literal request, flagged as an addition): a single-tick implausible-jump detector (>1m between consecutive ~0.1s polls implies >10m/s, physically impossible for this robot) — a closer match to the previously-documented discrete map→odom jump failure mode than the cumulative check alone. **Known limitation, stated in the new docstring:** this cumulative/jump-based check is coarser than v3's old absolute-bounds check — it can no longer directly say "you are outside the field," only "you've traveled implausibly far" or "you just teleported."
  - **Caveat this rewrite reintroduces, stated explicitly in the new docstring:** v3 was deliberately spawn-agnostic (read whatever pose TF gave it, assumed nothing). v4 cannot be — without a world-frame reference, computing correct real-world leg distances requires assuming the robot is actually at the known spawn pose when `stage_perimeter()` starts. If this script is ever launched after the robot has already been driven around post-SLAM-activation, the computed distances will be wrong. Inherent to relative-only tracking during initial mapping, not an oversight.
  - First pose is still logged at startup (still useful to confirm TF is alive) but the log message now explicitly states it's expected to read close to `(0,0,0)`, not the real spawn — correct behavior, not a bug.
  - Rebuilt (`colcon build --packages-select robot_bringup`) — clean, no errors/warnings. Syntax-checked (`py_compile`) before building. Installed copy confirmed identical to source (`diff` clean). **Not run live** — the rewrite itself was the task; live verification is next, separately.
  - **`scout_mission.py` checked for the same class of bug — REPORT ONLY, file not touched.** Structurally it's fine: no raw TF polling anywhere (confirmed by grep — only builds `frame_id='map'` `PoseStamped` goals via `pose()`/`build_waypoints()` and hands them to `nav.followWaypoints()`), so it doesn't have v3's literal bug (comparing live pose to a mismatched frame's constants) — Nav2/AMCL resolves `map`-frame goals internally, which is the correct, designed-for way to use that frame.
    - **However, a related, non-obvious problem was found by tracing the numbers through, and it's real:** `scout_mission.py`'s coordinate constants (`AISLES`, `Y_SOUTH`/`Y_NORTH`, `SPAWN`) are Gazebo WORLD-frame numbers (they're literally copied from `farm.sdf`'s row/wall geometry) sent directly as `map`-frame goal coordinates. That's only correct if the saved map's frame happens to coincide with world frame — which requires the SLAM session that built the map to have started with the robot at world `(0,0)` facing world `+x` (yaw 0). It didn't: the real spawn is `(-9.5,-10)` facing north (yaw 90°), so a map built starting there is **offset by `(-9.5,-10)` and rotated 90°** relative to world frame. Worked the transform by hand (`world_x = SPAWN_X − map_y`, `world_y = SPAWN_Y + map_x`, derived from the known 90° yaw at map origin) and checked it against `scout_mission.py`'s actual goals: the **first waypoint** (`map` x=−6.0, y=−10.0) resolves to **world (0.5, −16.0) — 4m past the real south wall**; the **"return to spawn" waypoint** (`map` x=−9.5, y=−10.0) resolves to **world (0.5, −19.5) — ~10m past the wall**. Nav2 would very likely reject these outright (outside the loaded costmap) rather than drive somewhere quietly wrong — a different failure signature than `slam_coverage_drive.py`'s silent undercoverage, but a real, confirmed bug, same root cause (constants written assuming map frame ≡ world frame, false given the non-zero spawn heading).
    - **Caveat on this finding:** the exact numbers above assume Phase 1 (SLAM remap, not yet run) starts fresh from the current real spawn `(-9.5,-10, 90°)` — the intended/planned procedure per this file's own "Next action" line. If Phase 1 is ever done differently, the specific numbers change, but the structural problem (map frame not aligned with world frame given a non-zero-heading spawn) remains.
    - **Not fixed, not touched — flagged only, per instruction.** Worth resolving before Phase 3 (`scout_mission.py`'s actual run) can be trusted; likely fix shape is the same relative/local-frame rethink just applied to `slam_coverage_drive.py`, or issuing goals relative to AMCL's own reported pose rather than hardcoded world-derived numbers — decision deferred to the user.
  - **DID NOT TOUCH:** `farm.sdf`, `nav2_params.yaml`, SLAM map files, the report.
- **Resource-starvation finding, 2026-08-19: system load hit 26.73 with 373MB free RAM running Gazebo + full Nav2 (7 nodes) + slam_toolbox + RViz together, causing TF to lag ~17s behind sim time** (looked like a bug, was actually starvation). Root cause: `slam_coverage_drive.py` drives via direct `/cmd_vel_teleop` publishing — it never touches Nav2 — so the full Nav2 stack was running for zero benefit during mapping. Added a lighter mapping-only launch path.
  1. **`full_system.launch.py` inspected — confirmed `use_slam:=true` unconditionally brings up SLAM *and* full Nav2 together, no existing lighter path.** `full_system.launch.py` includes `navigation.launch.py` unconditionally (via a 10s `TimerAction`), passing `use_slam` through. Inside `navigation.launch.py`, the 7 shared Nav2 nodes (`controller_server`, `smoother_server`, `planner_server`, `behavior_server`, `bt_navigator`, `waypoint_follower`, `velocity_smoother`) are appended **before** the `if use_slam:`/`else:` branch even runs — that branch only decides whether to *additionally* add `slam_toolbox` (SLAM) or `map_server`+`amcl` (AMCL). So `use_slam:=true` layers SLAM on top of the full Nav2 stack, never in place of it.
  2. **Added `nav2` launch argument (default `true`) to both `navigation.launch.py` and `full_system.launch.py` — nothing else changes behavior by default.** When `nav2:=false`, `navigation.launch.py`'s `launch_setup()` takes an early-return path before the existing "Shared Nav2 nodes" block: skips all 7 Nav2 nodes and the AMCL/map_server branch entirely, brings up only `slam_toolbox` (if `use_slam:=true`) under its own minimal `lifecycle_manager_slam_only` (needed since `slam_toolbox` is a `LifecycleNode` — it stays UNCONFIGURED and does nothing without something driving it to ACTIVE; the existing full lifecycle manager was skipped along with everything else it would otherwise manage). Gazebo, `robot_state_publisher`, and the ROS↔Gz bridge (all in `simulation.launch.py`) were untouched — never conditional, exactly as instructed. Everything after the early-return in `navigation.launch.py` (the full-Nav2 branch) is byte-for-byte unchanged from before, so `nav2:=true` (the default) reproduces the exact prior behavior.
     - **New quick-start for mapping runs:** `ros2 launch robot_bringup full_system.launch.py use_slam:=true nav2:=false`.
     - Rebuilt (`colcon build --packages-select robot_bringup`) — clean. Verified both launch files parse and expose the new `nav2` arg correctly via `ros2 launch robot_bringup full_system.launch.py --show-args` / `... navigation.launch.py --show-args` (both list `'nav2': false = skip the full Nav2 stack, slam_toolbox only (default: 'true')`). Installed copies confirmed identical to source (`diff` clean on both files).
  3. **Gazebo headless option checked — REPORT ONLY, none added, per instruction.** No `gui`/`headless` launch argument exists in this project's `simulation.launch.py` — it hardcodes `gz_args: ['-r ', world_file]` (run, no rendering-disable flag) with no other Gazebo launch args exposed. The underlying `ros_gz_sim`/`gz_sim.launch.py` (`/opt/ros/jazzy/share/ros_gz_sim/launch/gz_sim.launch.py`) doesn't add a `gui`/`headless` arg of its own either — it just passes whatever string is given as `gz_args` straight through to the `gz sim` CLI. Confirmed via `gz sim --help` that the underlying CLI does support headless: `-s` runs server-only (headless mode; overrides `-g` if both given), `-g` runs GUI-only. So a real headless option is reachable in principle (e.g. `gz_args: ['-r -s ', world_file]`), it's just not currently wired up as a launch argument anywhere in this project. Not added — out of scope for this task, reported only.
  4. **`arbitration_node` CPU usage (observed ~78%) checked — REPORT ONLY, not fixed.** Read the full node (`mode_arbitration/mode_arbitration/arbitration_node.py`). Found nothing that loops or retries aggressively on error: two subscription callbacks (`_auto_cb`, `_teleop_cb`) just store the latest message, `_mode_cb` does a simple string compare with no retry path, and the two timers (`_forward_cb` at 20Hz, `_status_cb` at 1Hz) each do a single trivial publish with no blocking calls, no `while` loops, no busy-wait, no exponential backoff. `main()`'s shutdown sequence (`node.destroy_node()` then `if rclpy.ok(): rclpy.shutdown()`) is in the conventional safe order. **Nothing in this file's own logic explains sustained 78% CPU for what it does.** Given the same session already diagnosed 373MB-free/load-26.73 system-wide resource starvation as the real cause of an unrelated symptom (17s TF lag) that "looked like a bug but wasn't," the same explanation is the more likely one here too — an inflated %CPU reading on essentially any process, arbitration_node included, during that measurement window, rather than an inefficiency specific to this node. **Recommend re-measuring `arbitration_node` in isolation (or under the new `nav2:=false` lighter launch) before concluding its own code needs fixing.**
     - **On the separate `RuntimeError: Unable to convert call argument '0' to Python object` crash seen during an earlier shutdown:** no plausible connection found to this file's Python-level code — there's no manual type conversion, no unusual argument passing, nothing that would construct a literal `'0'` argument anywhere in this node. This error signature matches a known class of rclpy/pybind11 shutdown-race issue (a C-extension object — commonly a timer — being torn down while still in flight), not something traceable to a specific line here. The 20Hz `_forward_cb` timer's short period increases the odds of a callback being mid-flight at the exact instant shutdown is triggered, which is a plausible *trigger* for hitting an existing rclpy/Jazzy race, but not evidence of a bug in this node's own logic. If it recurs, capturing the full traceback (not just the one-line message) and noting whether it was a SIGINT-driven shutdown (vs. `kill -9`, which wouldn't raise a Python exception at all) would help narrow it down — not attempted here, per instruction to report only.
  - **DID NOT TOUCH:** `farm.sdf`, `nav2_params.yaml`, SLAM map files, the report, `slam_coverage_drive.py`, `scout_mission.py`.

## How to use this file

- **Starting a new Claude Code session?** Tell it: *"Read PROJECT_STATUS.md in this repo root first, then do [X]."* Don't re-explain the project — the file does that.
- **Ending a session (even an unfinished one)?** Before closing, update the CURRENT STATE block above — what got done, what's next, anything blocking. Takes 30 seconds, saves you re-deriving it later.
- **Lost track of where you are?** Read CURRENT STATE. That's the whole answer.
- Keep this file in the repo (`~/ros2_ws/src/PROJECT_STATUS.md`) and commit it — unlike AUDIT.md, this one's meant to be tracked, it's just status, not an audit trail.

---

## Presentation

**Date:** August 19, 2026 — live demo + results/graphs, to your supervisor.
**Scope decision:** full protocol, all 5 scenarios × 10 trials = 50 runs. No scope cuts. Whatever's complete by the 19th is what gets demoed — the phases below aren't scheduled against the date, they just run in order as fast as they go.

---

## Phases (full protocol, dependency-ordered)

### Phase 0 — Sanity check what's already built
Confirms the packages already marked "done" actually work, before building the SLAM remap on top of them.
- [ ] All 6 packages build clean: `cd ~/ros2_ws && colcon build && source install/setup.bash` — no errors
- [ ] Gazebo launches and TurtleBot3 Waffle spawns visibly at (0,−11,0.2) in the farm world: `ros2 launch robot_bringup simulation.launch.py`
- [ ] Farm world looks right — 5 rows, 10 plants each, 3 obstacles, walls — quick visual check against the spec, not a rebuild
- [ ] `/scan`, `/odom`, `/tf` are all publishing: `ros2 topic hz /scan`, `ros2 topic hz /odom`, `ros2 topic echo /tf --once`
- [ ] Keyboard teleop moves the robot: `ros2 run teleop_node keyboard_teleop`, confirm WASD/arrows respond
- [ ] Mode arbitration is alive and defaults to MANUAL: `ros2 topic echo /current_mode --once`
- [ ] Manual → auto → manual switch works via `/mode` topic publish, confirm `/current_mode` actually changes
- [ ] `scout_mission` binary is registered (won't run correctly without a map yet, but confirm it's found): `ros2 run robot_bringup scout_mission` — expect it to fail waiting on Nav2/map, not a "package not found" error

If anything here fails, fix it before Phase 1 — the SLAM remap assumes all of this already works.

### Phase 1 — SLAM remap ⬅ *next up after Phase 0*
- [ ] Drive full coverage (south strip + confirm east perimeter reaches x=+11)
- [ ] Save map, verify coverage numerically against known field bounds

### Phase 2 — AMCL test
- [ ] Set initial pose near (0,−11)
- [ ] Confirm particle convergence (check `/amcl_pose` covariance, not just visual)

### Phase 3 — Autonomous navigation test
- [ ] Single 2D Nav Goal, confirm robot reaches it
- [ ] Full `scout_mission.py` run, all 11 waypoints

### Phase 4 — Mode-switch test
- [ ] Trigger manual override mid-navigation, confirm clean stop + handover
- [ ] Switch back to auto, confirm clean resume

### Phase 5 — Quick decisions + fixes (cheap, do in parallel with 1–4 if you want)
- [ ] **Decision:** robot model — document TB3 Waffle as-simulated, or swap in the custom URDF?
- [ ] **Decision:** auto-trigger switching — build a minimal version, or rewrite Methodology 3.7.1 + Recommendation 5.2(ii) to match the real manual-only mechanism?
- [ ] License check: `cat ~/ros2_ws/src/virtual_maize_field/LICENSE` (MIT vs GPLv3)
- [ ] Docstring fix: "5s" → "10s" in `full_system.launch.py`
- [ ] GitHub: switch default branch to `main`, then delete remote `master`

### Phase 6 — Define the 5 scenarios
- [ ] Concrete trigger mechanism for each: normal run, obstacle-in-row, narrow passage, sensor disturbance, dynamic obstacle stress test — not just descriptions, actual implementable mechanisms

### Phase 7 — Build trial infrastructure
- [ ] `record_trial.sh` — name + start rosbag2 recording per trial
- [ ] `trial_reset.py` — reset robot to spawn between trials
- [ ] `analyse_trials.py` — extract metrics from bags → CSV → charts

### Phase 8 — Dry-run validation
- [ ] One trial per scenario (5 runs total) to confirm the scripts actually work end-to-end before committing to all 50

### Phase 9 — Run the experiment
- [ ] All 50 trials (5 scenarios × 10 reps), rosbag2 recording each

### Phase 10 — Analysis
- [ ] Run `analyse_trials.py` on the full dataset
- [ ] Generate the tables/charts the report needs

### Phase 11 — Report
- [ ] Apply Phase 5 decisions to Methodology 3.7.1 / Recommendation 5.2(ii) and Section 3.4 (Robot Model)
- [ ] Section 3.2 (Development Tools) — versions + screenshots
- [ ] Figures — conceptual ones anytime, system screenshots once Phase 1 is done
- [ ] Chapter 4 (Results) — from Phase 10 data
- [ ] Chapter 5 (Conclusion) + Abstract results summary
- [ ] Front matter (dedication, acknowledgement, HOD signature)

### Loose ends (not gating anything, close out when convenient)
- [ ] Confirm the 3 proposal fixes (page trim, citation format, Gantt shading) actually made it into the submitted proposal
